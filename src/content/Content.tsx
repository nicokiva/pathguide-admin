import * as React from "react";
import styled from "styled-components";

const ContentWrapper = styled.div`
  overflow: auto;
`;

export const Content: React.FC = () => {
  return <ContentWrapper>sdsdsd</ContentWrapper>;
};
